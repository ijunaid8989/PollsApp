<div class="container topical">
	<div class="col-xs-6">
		<%= form_for @poll do |f| -%>
			<div class="form-group">
				<%= f.text_field :title , placeholder: "Title", class: "form-control" %>
			</div>
			<div class="form-group">
				<%= f.submit "Create", class: "btn btn-success" %>
			</div>
		<% end -%>
	</div>
</div>

<h1>All Polls</h1>
<ul class="list-group">
	<% @polls.each do |poll| %>
	  <li class="list-group-item">
		    <span class="badge"><%= link_to "show" ,  %></span>
		    <%= poll.title %>
	  </li>
  <% end -%>
</ul>